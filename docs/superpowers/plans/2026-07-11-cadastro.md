# Plano — Cadastro (signup + trial 30 dias)

Spec: `docs/superpowers/specs/2026-07-11-cadastro-design.md`

1. **Funções puras (TDD)** — `src/utils/cadastro.test.js` (TZ SP na 1ª linha):
   validarCadastro (válido; nome vazio; email inválido; senha <6; confirmação
   diferente; múltiplos erros) e calcularFimTeste (normal, vira mês, vira ano,
   fevereiro bissexto). Teste falha → implementa `src/utils/cadastro.js` →
   passa.
2. **Migração** — `supabase/migrations/20260711_cadastro_trial.sql`: colunas
   `plano`/`teste_termina_em` + trigger security definer. Aplicar em produção
   só com autorização explícita do usuário.
3. **useAuth** — `cadastrar(email, senha, nomeLoja)` com signUp + metadados +
   mapeamento de erros PT-BR. Retorna `{ precisaConfirmarEmail }`.
4. **UI** — `src/pages/Cadastro.jsx` (lazy) espelhando Login.jsx; link no
   Login (`aoCriarConta`); `telaAuth` no App.jsx; `vercel.json` SPA rewrite.
5. **Verificação** — suíte inteira + build; conferir critérios 1–7; ledger.
